// TODO: implement a function that prints errors
export function PrintFormErrors(errors: string[]) {
	console.log(errors)

    const name = document.getElementsByName('name');
    console.log(name);
    return;
}
